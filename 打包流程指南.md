# 打包流程指南（AI助手专用）

## 打包前检查
1. 确认 `package.json` 版本号已更新
2. 确认 `CHANGELOG.md` 已添加新版本记录
3. 确认 `HelpModal.jsx` 中的 `CHANGES` 数组已添加新版本
4. 确认 `Home.jsx` 底部版本号使用 `import.meta.env.VITE_APP_VERSION`

## 正式版打包流程

### 步骤1：确保 better-sqlite3 是 x64 版本
```bash
npx electron-rebuild -f -w better-sqlite3
```

### 步骤2：打包 x64 正式版（自动混淆）
```bash
npm run fast:build
```

### 步骤3：打包 ia32 正式版（自动混淆）
```bash
npm run fast:build:ia32
```

### 步骤4：恢复开发环境
```bash
npx electron-rebuild -f -w better-sqlite3
```

## 测试版打包流程（跳过混淆，仅开发测试）
```bash
npm run fast:build:skip
npm run fast:build:ia32:skip
```

## 输出目录
- x64: `D:\2\release-x64\`
- ia32: `D:\2\release-ia32\`

## 常见错误处理

### 错误1：EBUSY: resource busy or locked
**原因**：文件被占用（应用正在运行或杀毒软件锁定）
**解决**：
```powershell
Stop-Process -Name "小说创作工坊" -Force 2>$null
Stop-Process -Name "electron" -Force 2>$null
Start-Sleep -Seconds 2
```

### 错误2：打包后页面卡在加载
**原因**：打包 ia32 后 better-sqlite3 被重编译为 32 位
**解决**：
```bash
npx electron-rebuild -f -w better-sqlite3
```

### 错误3：找不到 Windows SDK
**原因**：better-sqlite3 需要编译但缺少 SDK
**解决**：在 `electron-builder.yml` 中设置 `npmRebuild: false`

### 错误4：Cannot find module 'undici'
**原因**：Electron 打包后不能直接 require Node.js 内置模块
**解决**：改用 `node-fetch` 和 `https-proxy-agent`

### 错误5：版本号显示不正确
**原因**：硬编码版本号
**解决**：
- `Home.jsx` 底部使用 `import.meta.env.VITE_APP_VERSION`
- `AboutTab.jsx` 使用 `import.meta.env.VITE_APP_VERSION`
- `vite.config.js` 中配置 `define` 注入版本号

### 错误6：更新日志不显示新版本
**原因**：`HelpModal.jsx` 中 `CHANGES` 数组未更新
**解决**：在数组开头添加新版本记录

## 详细错误排查
参考：`D:\2\bug修复手册-整理版.md`